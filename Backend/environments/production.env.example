NODE_ENV=production
OPENAI_TEXT_MODEL=gpt-5.6-luna
OPENAI_IMAGE_MODEL=gpt-image-2
WORKER_CONCURRENCY=2
JOB_LEASE_SECONDS=120
# Production infrastructure is intentionally not provisioned until staging passes.
# Add URLs, database credentials, API keys, and cost rates as Render secrets.
